<div>
  <div class="flex gap-3">
    <div>
      <%= render 'love_letter/players/partials/stats', player:, game:, phaser_config: @phaser_config %>
    </div>
    <div>
      <%= render 'love_letter/players/partials/rewards', player:, game: %> 
      <%= render 'love_letter/players/partials/current_hand', player:, game: %>
    </div>
  </div>
</div>

<div tabindex="0" class="collapse collapse-arrow border-base-300 bg-base-200 border">
  <div class="collapse-title text-xl font-medium">History</div>
  <div class="collapse-content">
    <%= render 'players/partials/history', game: %> 
  </div>
</div>
<%= render 'love_letter/players/partials/templates' %>


<script>
  function executeAction(e) {
    let data = { type: e.dataset.type, player_index: <%= player.dig('index') %> }
    if (!!e.dataset.name) data.value = e.dataset.name
    window.app.action.send(data);
  }

  document.addEventListener('DOMContentLoaded', function () {
    HtmlActions.addHoverCardFunctions()
  });
</script>
