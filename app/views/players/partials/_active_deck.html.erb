Active Deck 
<div class='grid grid-cols-5 gap-1' id='active-cards'>
  <% game.data.dig('deck', 'active').each_with_index do |card, index| %>
    <%= render 'players/components/card', card:, id: "active-card-#{index}" %>
  <% end %>
</div>
