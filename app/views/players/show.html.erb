<div class="mx-2 overflow-hidden">
  <div class="flex">
    <div class="flex-auto">
      <div class="flex">
        <%= render 'players/partials/current_hand', player: @player, game: @game %>
        <%= render 'players/partials/stats', player: @player %> 
      </div>
      <%= render 'players/partials/chat', player: @player %> 
    </div>
    <%= render 'players/partials/game', game: @game, player: @player %> 
  </div>
</div>
